data:
  train_pairs: data/pairs.jsonl
  index_items: data/index_items.jsonl

model:
  input_dim: 4
  hidden_dim: 16
  output_dim: 8
  dropout: 0.1

training:
  epochs: 20
  batch_size: 16
  lr: 0.001

paths:
  checkpoint: checkpoints/short_siamese/best.pt
  index: indexes/short_index.json
